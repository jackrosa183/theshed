<div class="flex flex-col justify-center mx-48 gap-4">
  <p style="color: green"><%= notice %></p>
  <%= render @tune %>
  <div class="mx-auto flex gap-2">
    <%= link_to "Back to tunes", tunes_path, class: "btn btn-secondary" %>
    <%= link_to "Edit", edit_tune_path(@tune), class: "btn btn-secondary" %>
  </div>
</div>
